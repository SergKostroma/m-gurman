// // Import vendor jQuery plugin example
// import '~/app/libs/mmenu/dist/mmenu.js'

document.addEventListener('DOMContentLoaded', () => {



	// const redButtons = document.querySelectorAll('.red-button')
	// for (let redButton of redButtons){
	// 	redButton.addEventListener ('click', redButtonActivizer)
	// 	function redButtonActivizer(){
	// 		redButton.classList.toggle('red-button-active')
	// 	}
	// } 


	// const whiteButtons = document.querySelectorAll('.white-button');
	// for (let whiteButton of whiteButtons) {
	// 	document.addEventListener('click', whiteButtonActivate)

	// 	function whiteButtonActivate(event) {
	// 		if (event.target.classList.contains('white-button')) {
	// 			event.target.classList.toggle('white-button-active')
	// 		} else {
	// 			whiteButton.classList.remove('white-button-active')
	// 		}
	// 	}
	// }


	//  const redButtons = document.querySelectorAll('.red-button');

	//  for (let redButton of redButtons) {	
	//  	document.addEventListener('click', redButtonActivate)

	//  	function redButtonActivate(event) {
	//  		if (event.target.classList.contains('red-button')) {
	//  			event.target.classList.add('red-button-active')
	//  		} else {
	//  			redButton.classList.remove('red-button-active')
	//  		}
	//  	}
	//  }

    



})